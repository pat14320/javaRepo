package abstractClasses;

public interface AnimalStuff {
	// These values will apply to every class that implements this interface.
	// That's why it typically doesn't make sense to define fields in an abstract class
//	int age = 1;
//	String name = "Larry";
	
	// Don't need abstract keyword here
	public void poop();
}
